# 	以'#'开始的行，被视为注释.                                                                                                                          
# 	.gitignore文件对其所在的目录及所在目录的全部子目录均有效。
#  	通过将.gitignore文件添加到仓库，其他开发者更新该文件到本地仓库，以共享同一套忽略规则。


# 忽略掉所有文件名是 foo.txt的文件.

#	foo.txt

# 忽略所有生成的 html文件,

#	*.html

# foo.html是手工维护的，所以例外.

#	!foo.html

# 忽略所有.o和 .a文件.

	*.[oa]

Project.*

#	配置语法：以斜杠“/”开头表示目录；以星号“*”通配多个字符；
#	以问号“?”通配单个字符以方括号“[]”包含单个字符的匹配列表；
#	以叹号“!”表示不忽略(跟踪)匹配到的文件或目录；

